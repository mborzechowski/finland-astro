---
import { HomeIcon, BanIcon, SparklesIcon, CubeIcon, FireIcon } from "@heroicons/react/solid";


const images = [
  "/cabin-main.jpg", // główne zdjęcie
  "/cabin-1.jpg",
  "/cabin-2.jpg",
  "/cabin-3.jpg",
  "/cabin-4.jpg",
];
---

<section class="bg-gray-100 py-12 mb-60">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
      <div class="grid grid-cols-2 gap-4">
        <div class="col-span-2">
          <img
            src={images[0]}
            alt="Main cabin view"
            class="rounded-lg object-cover w-full h-full shadow-md"
          />
        </div>
        {images.slice(1).map((image, index) => (
          <img
            src={image}
            alt={`Cabin detail ${index + 1}`}
            class="rounded-lg object-cover w-full h-full shadow-sm"
          />
        ))}
      </div>

      
      <div class="flex flex-col justify-center">
        <h2 class="text-2xl md:text-3xl font-bold text-gray-800 mb-4">
          Holiday Home Levin Gaissa by Interhome
        </h2>
        <p class="text-gray-700 mb-6 leading-relaxed">
          The Holiday Home Levin Gaissa by Interhome is located in Levi. The
          distance to major attractions: Levi Spa Water World - less than a
          kilometer, Congress & Exhibition Centre Levi Summit - 5.3 km. The
          area offers excellent skiing opportunities. Distance to key points of
          interest: Peak Lapland Viewing Deck - 7 km, Mary's Chapel, Levi - 1.2
          km.
        </p>
         <ul class="space-y-2">
        <li class="flex items-center">
          <CubeIcon  className="h-5 w-5 text-finland-blue mr-2" />
          <span>56 m2 area</span>
        </li>
        <li class="flex items-center">
          <BanIcon className="h-5 w-5 text-finland-blue mr-2" />
          <span>Non-smoking rooms</span>
        </li>
        <li class="flex items-center">
          <SparklesIcon className="h-5 w-5 text-finland-blue mr-2" />
          <span>Kitchenette</span>
        </li>
        <li class="flex items-center">
          <HomeIcon  className="h-5 w-5 text-finland-blue mr-2" />
          <span>Bath or shower</span>
        </li>
        <li class="flex items-center">
          <FireIcon className="h-5 w-5 text-finland-blue mr-2" />
          <span>Sauna</span>
        </li>
      </ul>
        <a
          href="/cabin"
          class="inline-block px-6 py-3 text-base mt-4 font-medium text-white bg-gray-900 rounded-md hover:bg-gray-700"
        >
          See more
        </a>
      </div>
    </div>
  </div>
</section>